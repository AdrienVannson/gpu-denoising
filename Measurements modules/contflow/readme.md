# Control flow: Measure the performance impact of thread divergence

## connectors

* `input` Not used
* `output` Not used

## parameters

* `nb_cond0` The number of threads that will execute the first condition at the beginning
